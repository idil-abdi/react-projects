import { Alert } from "react-bootstrap";

export const AlertBanner = () => {
    return (
        <Alert variant="warning">
            Please select a valid organisation or username.
        </Alert>
    );
};