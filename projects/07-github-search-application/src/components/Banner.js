import { Stack } from "react-bootstrap"

export const Banner = () => {
    return (
        <Stack className="text-center">
            <h1>GitHub Search</h1>
            <div>A tool to explore the various respositories by username and organisation using GitHub API.</div>
        </Stack>
    );
};